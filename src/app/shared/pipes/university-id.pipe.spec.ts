import { UniversityIdPipe } from './university-id.pipe';

describe('UniversityIdPipe', () => {
  it('create an instance', () => {
    const pipe = new UniversityIdPipe();
    expect(pipe).toBeTruthy();
  });
});
